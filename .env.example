# Database Configuration
POSTGRES_DB=mytourdb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
DB_PORT=5432

# Application Configuration
APP_PORT=8080
UI_PORT=4200
CORS_ALLOWED_ORIGINS=http://localhost:4200

# OpenRouteService Directions API
# Leave ORS_API_KEY empty for local demo fallback routing.
ORS_ENABLED=true
ORS_BASE_URL=https://api.openrouteservice.org
ORS_API_KEY=
ORS_CONNECT_TIMEOUT=3s
ORS_READ_TIMEOUT=10s
ORS_BIKE_PROFILE=cycling-regular
ORS_HIKE_PROFILE=foot-hiking
ORS_RUNNING_PROFILE=foot-walking
ORS_VACATION_PROFILE=driving-car

# Open-Meteo Weather API
# Leave enabled for automatic weather snapshots; local fallback data is used if the upstream is unavailable.
OPEN_METEO_ENABLED=true
OPEN_METEO_ARCHIVE_BASE_URL=https://archive-api.open-meteo.com
OPEN_METEO_FORECAST_BASE_URL=https://api.open-meteo.com
OPEN_METEO_CONNECT_TIMEOUT=3s
OPEN_METEO_READ_TIMEOUT=10s
OPEN_METEO_FORECAST_PAST_DAYS=92
OPEN_METEO_FORECAST_DAYS=16

# Filesystem cover-image storage
# The database stores only the returned metadata/path reference, not binary image data.
IMAGE_STORAGE_BASE_DIRECTORY=/tmp/mytour-cover-images
IMAGE_MAX_SIZE_BYTES=5242880
IMAGE_ALLOWED_CONTENT_TYPES=image/jpeg,image/png,image/webp
